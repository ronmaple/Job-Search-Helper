{
  "dependencies": {
    "dotenv": "^8.2.0"
  },
  "scripts": {
    "dev": "concurrently --kill-others \"cd client && yarn start\" \"cd server && nodemon index.js\""
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}